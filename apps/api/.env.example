# Core Configuration
NODE_ENV=production
API_VERSION=1.0.0
LOG_LEVEL=info
PORT=4000

# Blockchain Configuration
CHAIN_ID=8453
PAYMENT_ROUTER_ADDRESS=0x...
SERVER_SIGNER_PK=0x...

# Security Configuration
ALLOWED_ORIGINS=https://example.com,https://www.example.com,https://api.example.com
VALID_API_KEYS=key1,key2,key3
WEBHOOK_SECRET=your-webhook-secret-here

# Custom Domain (optional)
API_DOMAIN=api.example.com

# Database Configuration (if using)
DATABASE_URL=postgresql://user:password@localhost:5432/paykit

# Redis Configuration (for production rate limiting)
REDIS_URL=redis://localhost:6379

# Monitoring (optional)
SENTRY_DSN=
DATADOG_API_KEY=

# Build Information (auto-populated in CI/CD)
BUILD_TIME=
VERCEL_GIT_COMMIT_SHA=
